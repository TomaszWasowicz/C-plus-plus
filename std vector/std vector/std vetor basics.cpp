#include <iostream>
#include <vector>

int main()
{
	std::vector<int>array{9, 7, 5, 3, 1}; //no need to set the size[]
	std::vector<int>array2{ 9,7,5,3,1 };
	std::vector<char>array4{ 'r','t','y','p' };


	std::vector array3{ 9,7,5,3,1 };

	array[0] = 2;

	array={ 10,2,3,4,5,6,8 }; // resizing the array

	std::cout << array[0];



	return 0;
}